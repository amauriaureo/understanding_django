from django.db import models

# Create your models here.
# São as classes que representam uma tabela no banco de dados.
# Elas fazem a comunicação de Python com o banco de dados
# de uma forma mais abstrata
# (não precisamos escrever o código SQL para consultas, inserções, atualizações e remoções)
